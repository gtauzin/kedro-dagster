CONFIG_LOADER_ARGS = {
    "base_env": "base",
    "default_run_env": "local",
    "config_patterns": {
        "dagster": ["dagster*", "dagster/**"],  # configure the pattern for configuration files
    },
}
